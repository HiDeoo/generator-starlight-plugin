---
title: <%= name %>
description: <%= description %>
head:
  - tag: title
    content: <%= name %>
  - tag: style
    content: |
      .hero-html {
        --size: 12rem;
        font-size: var(--size);
        justify-content: center;
        line-height: var(--size);
      }
template: splash
editUrl: false
hero:
  tagline: <%= description %>
  image:
    html: '<%= emoji %>'
  actions:
    - text: Get Started
      link: /getting-started/
      icon: right-arrow
<% if (theme) { %>    - text: Examples
      link: /examples/kitchen-sink/
      icon: right-arrow
      variant: minimal
<% } -%>
---

import { Card, CardGrid } from '@astrojs/starlight/components'

## Next steps

<CardGrid stagger>
  <Card title="Install the <% if(theme){ %>theme<% } else{ %>plugin<% } %>" icon="puzzle">
    Check the [getting started guide](/getting-started/) for installation instructions.
  </Card>
  <Card title="Configure the <% if(theme){ %>theme<% } else{ %>plugin<% } %>" icon="setting">
    Edit your config in the `astro.config.mjs` file.
  </Card>
<% if (theme) { %>  <Card title="Preview the theme" icon="seti:image">
    Check out the [example pages](/examples/kitchen-sink/) to see the theme in action.
  </Card>
  <Card title="Read the docs" icon="open-book">
    Learn more in the [`<%= name %>` documentation](/getting-started/).
  </Card>
<% } -%>
</CardGrid>
